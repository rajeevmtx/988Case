import { LightningElement ,track,api} from 'lwc';
import phoneIcon from '@salesforce/resourceUrl/Phone988';

export default class CmCallScriptInit extends LightningElement {
    phoneIcon = phoneIcon;
}